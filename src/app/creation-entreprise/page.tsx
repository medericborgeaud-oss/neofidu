import CreationEntreprisePage from "./CreationEntrepriseClient";

// Metadata is defined in layout.tsx for better organization

export default function Page() {
  return <CreationEntreprisePage />;
}
